services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: kftray-docs:latest
    container_name: kftray-docs
    ports:
      - "3030:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - PORT=3000
